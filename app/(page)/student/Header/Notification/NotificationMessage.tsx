import { INotificationData } from '@/app/types/student/notification.type';
export default function NotificationMessage({ noti }: { noti: INotificationData }) {
    return (
        <div>

        </div>
    )
}